services:
  app:
    container_name: fechttrainer-app
    image: ${APP_IMAGE:?APP_IMAGE is required}
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: "3000"
      STATIC_DIR: /app/artifacts/sabre-training/dist/public
    expose:
      - "3000"
    restart: unless-stopped

  caddy:
    container_name: fechttrainer-caddy
    image: caddy:2-alpine
    depends_on:
      - app
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
